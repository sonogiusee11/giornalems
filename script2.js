// Example JS to handle dynamic behaviors (e.g., fetching news data)
document.addEventListener('DOMContentLoaded', function () {
    console.log('Pagina caricata correttamente!');

    // Add any JS functionality you want for interactive components (e.g., comment section, sliders, etc.)
});
